#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
const ll mod = 1e9+7;

int main()
{
	vector <int> example;
	example.push_back(3);
	example.push_back(10);
	example.push_back(33); // Adds 3,10,33 adds elements from back
	for (int i = 0; i < example.size(); ++i)
	{
		cout<<example[i]<<" ";
	}
	if (!example.empty()) // checks if empty
	{
		example.clear(); //clears vector
	}

	vector <int> another_vector;
	another_vector.push_back(10);


	//Iterators
	


	return 0;
}